{
  "name": "advanced-features-example",
  "version": "1.0.0",
  "private": true,
  "description": "Advanced Rill features: metrics, timeout, requireWhitelist, performance monitoring",
  "scripts": {
    "build": "rill build src/plugin.tsx -o dist/plugin.js",
    "build:dev": "rill build src/plugin.tsx -o dist/plugin.js --no-minify --sourcemap"
  },
  "dependencies": {
    "rill": "workspace:*"
  },
  "devDependencies": {
    "@types/react": "^18.2.0",
    "typescript": "^5.3.0",
    "rill-cli": "workspace:*"
  }
}
